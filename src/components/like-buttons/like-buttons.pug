mixin like-button(name, id, count)
    .like-button
        button.like-button__button(name= name id= id)
            .material-icons favorite_border
            span.like-button__count 
                !=count